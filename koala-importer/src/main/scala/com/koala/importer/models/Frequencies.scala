package com.koala.importer.models

case class Frequencies(
                        tripId: String,
                        startTime: String,
                        endTime: String,
                        headwaySecs: String
                      )
